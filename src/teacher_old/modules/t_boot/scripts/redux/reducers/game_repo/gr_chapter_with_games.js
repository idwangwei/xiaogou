import * as default_states from './../../default_states/index';
let gr_chapter_with_games = (state = default_states.gr_chapter_with_games, action = null)=> {
    switch (action.type) {
        default:
            return state;
    }
};

export  default gr_chapter_with_games;