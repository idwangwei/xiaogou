import * as default_states from './../../default_states/index';
let wr_gen_paper_processing=(state=default_states.wr_gen_paper_processing,action=null)=>{
    switch (action.type) {
        default:
            return state;
    }
};

export  default wr_gen_paper_processing;